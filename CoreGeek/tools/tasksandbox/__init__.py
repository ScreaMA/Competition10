"""本地任务沙盒：把沙盒里那套自进化任务环境在本地复刻出来。

对应设计文档V2 §6.2（"实测到的任务形态"）。

为什么需要它：自进化任务的全部故障都是**在沙盒里跑起来才暴露**的——脚本模板
的签名错误、`read()` 的换行归一、`locate()` 返回空文件表、鉴权头猜错……
`tests/test_scripts.py` 里的 stub 能覆盖一部分，但它是"按我们的想象搭的"；
这一套是按**对战日志原文 + 设计文档 §6.2 确证的事实**搭的，所以能回答
"换掉这段代码，任务真的能做完吗"。

四个模块：

    fixtures.py   任务原文 / API 文档 / 工作区规格（都是实测原文）
    apiserver.py  文化遗产查询服务（Bearer 鉴权 + location 参数 + 嵌套响应）
    env.py        把上面两样落成 /tmp/selfEvolutionTask/… 的目录结构
    run.py        命令行：建环境 → 生成脚本 → 真跑一遍 → 看标记
"""
