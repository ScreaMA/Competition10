# 代码改进清单

## 🔴 优先级1：必须立即修复

### 1. 创建run.sh启动脚本 ⚠️ **必需**

**原因**: 判题系统要求的启动方式是`bash run.sh <port>`

**位置**: `CoreGeek/run.sh`

**实现**:
```bash
#!/bin/bash
# run.sh - 启动客户端脚本
# 用法: bash run.sh <port>

if [ $# -ne 1 ]; then
    echo "Usage: bash run.sh <port>"
    exit 1
fi

PORT="$1"

# 检查Python版本
if ! command -v python3 &> /dev/null; then
    echo "Error: python3 not found"
    exit 1
fi

# 启动客户端
exec python3 main3.py "$PORT"
```

**验证**:
```bash
cd CoreGeek
chmod +x run.sh
./run.sh 8000
```

---

### 2. 修复日志体积问题

**问题**: `server.py:36` 记录完整请求体可能导致日志文件过大

**当前代码**:
```python
LOGGER.info("request_raw id=%d bytes=%d body=%s",
            req_id, length, raw.decode("utf-8"))
```

**修复方案**:
```python
# 在server.py顶部增加常量
MAX_LOG_BODY_LENGTH = 5000

# 修改第33-36行
body_str = raw.decode("utf-8")
if len(body_str) > MAX_LOG_BODY_LENGTH:
    body_str = body_str[:MAX_LOG_BODY_LENGTH] + f"...[truncated {len(body_str) - MAX_LOG_BODY_LENGTH} chars]"
LOGGER.info("request_raw id=%d bytes=%d body=%s", req_id, length, body_str)
```

**影响**: 防止单个请求撑满日志文件，保持调试信息的可读性

---

### 3. 增加围墙建造的边界检查

**问题**: `brain.py:_calc_wall_order()` 可能生成越界坐标

**当前代码**:
```python
return tuple(
    pos for pos in order
    if pos != entrance and turn.land(pos)
)
```

**修复方案**:
```python
return tuple(
    pos for pos in order
    if pos != entrance 
    and turn.land(pos)
    and 0 <= pos.x < turn.width 
    and 0 <= pos.y < turn.height
)
```

**位置**: `CoreGeek/src/agent/brain.py:428-431`

---

## 🟡 优先级2：重要但不紧急

### 4. 完善函数文档注释

**问题**: 部分复杂函数缺少详细说明

**需要补充的函数**:

#### 4.1 brain.py:_valid_stand_cells
```python
def _valid_stand_cells(
    turn: Turn,
    unit: Unit,
    target: Pos,
    claimed: set[Pos],
    inside_only: bool = False,
) -> list[Pos]:
    """计算目标周围可停留的格子
    
    参数:
        turn: 当前回合信息
        unit: 移动的单位
        target: 目标位置
        claimed: 已被其他单位声明的格子（防止碰撞）
        inside_only: 如果为True，只返回基地周围1格范围内的格子
    
    返回:
        按离基地距离排序的可停留格子列表（优先靠近基地）
    
    逻辑:
        1. 获取目标的八方向相邻格子
        2. 过滤掉非陆地、阻挡、已占用的格子
        3. 如果inside_only=True，只保留基地周围的格子
        4. 按离基地的距离排序
    """
```

#### 4.2 brain.py:_pair_controllers_and_weapons
```python
def _pair_controllers_and_weapons(turn: Turn) -> list[tuple[Unit, Unit]]:
    """为武器配对操控角色
    
    策略:
        - 按ID排序的角色列表 (worker1, worker2, pioneer)
        - 按位置排序的武器列表 (weapon1, weapon2, weapon3)
        - 使用zip()配对: (worker1->weapon1, worker2->weapon2, pioneer->weapon3)
    
    返回:
        [(controller, weapon), ...] 配对列表
    """
```

---

### 5. 增加单元测试

**创建测试目录**: `CoreGeek/tests/`

#### 5.1 test_protocol.py
```python
"""协议模块测试"""
import pytest
from agent.protocol import Pos, distance, manhattan_distance, Turn, Unit

def test_pos_distance():
    """测试切比雪夫距离"""
    assert distance(Pos(0, 0), Pos(3, 4)) == 4
    assert distance(Pos(0, 0), Pos(3, 3)) == 3
    assert distance(Pos(5, 5), Pos(5, 5)) == 0

def test_manhattan_distance():
    """测试曼哈顿距离"""
    assert manhattan_distance(Pos(0, 0), Pos(3, 4)) == 7
    assert manhattan_distance(Pos(0, 0), Pos(3, 3)) == 6

def test_pos_add():
    """测试坐标加法"""
    p1 = Pos(5, 10)
    p2 = p1 + (2, -3)
    assert p2 == Pos(7, 7)

def test_unit_backpack_full():
    """测试背包已满判断"""
    # 需要构造Unit对象进行测试
    pass
```

#### 5.2 test_grid.py
```python
"""地图模块测试"""
import pytest
from agent.grid import next_step, get_neighbors, cells_in_range
from agent.protocol import Pos, Turn, Unit

def test_get_neighbors():
    """测试八方向相邻格子"""
    neighbors = get_neighbors(Pos(5, 5))
    assert len(neighbors) == 8
    assert Pos(4, 4) in neighbors
    assert Pos(6, 6) in neighbors

def test_cells_in_range():
    """测试范围内格子"""
    cells = cells_in_range(Pos(5, 5), 2)
    assert Pos(5, 5) in cells
    assert Pos(3, 3) in cells
    assert Pos(7, 7) in cells
    assert Pos(2, 2) not in cells  # 超出范围
```

#### 5.3 test_brain.py
```python
"""决策模块测试"""
import pytest
from agent.brain import _calc_tower_sites, _calc_wall_order
from agent.protocol import Turn, Pos

def test_tower_sites():
    """测试武器塔位置计算"""
    # 需要构造Turn对象
    # 验证返回3个位置
    # 验证位置在基地周围一圈
    pass

def test_wall_order():
    """测试围墙建造顺序"""
    # 需要构造Turn对象
    # 验证围墙形成矩形
    # 验证留有入口
    pass
```

**运行测试**:
```bash
cd CoreGeek
pip install pytest
pytest tests/ -v
```

---

### 6. 完善开拓者任务系统

**当前状态**: brain.py:151有TODO标记

**实现步骤**:

#### 6.1 分析任务类型
根据docs/任务书.md，有三类任务：
1. **推理类**: 根据新闻推断矿石价格
2. **长上下文类**: 收集线索找到宝藏
3. **自进化类**: 沙盒环境执行命令

#### 6.2 实现任务领取逻辑
```python
def _pioneer_day_logic(
    turn: Turn,
    pioneer: Unit,
    tower_sites: tuple[Pos, ...],
    wall_order: tuple[Pos, ...],
    claimed: set[Pos],
    commands: dict[int, dict[str, Any]],
) -> None:
    """开拓者白天逻辑"""
    
    # 1. 检查是否有可领取的任务
    valid_tasks = [t for t in turn.player_tasks if t.is_valid]
    
    if valid_tasks:
        # 找到最近的任务点
        nearest_task = min(valid_tasks, key=lambda t: distance(pioneer.pos, t.task_position))
        
        # 如果在任务点旁边，领取任务
        if distance(pioneer.pos, nearest_task.task_position) <= 1:
            commands[pioneer.unit_id] = accept_task_command()
            return
        
        # 否则向任务点移动
        step = _step_toward(turn, pioneer, nearest_task.task_position, claimed)
        if step is not None:
            commands[pioneer.unit_id] = move_command(step)
            return
    
    # 2. 如果有任务正在进行，尝试完成任务
    # TODO: 实现任务完成逻辑（需要根据任务类型判断）
    
    # 3. 没有任务时，跟随武器塔（保持原有逻辑）
    weapons = turn.weapons()
    if not weapons:
        return
    
    nearest_weapon = min(weapons, key=lambda w: distance(pioneer.pos, w.pos))
    
    if distance(pioneer.pos, nearest_weapon.pos) <= 1:
        if pioneer.pos not in wall_order:
            return
    
    step = _step_toward(turn, pioneer, nearest_weapon.pos, claimed, inside_only=True)
    if step is not None:
        commands[pioneer.unit_id] = move_command(step)
```

**注意**: 任务系统需要解析`turn.phase_task`字段来判断当前任务状态

---

## 🟢 优先级3：可选优化

### 7. 实现资源交易策略

**场景**: 当石头过多时，卖掉换金币购买升级券

**位置**: `brain.py:_worker_day_logic`

**伪代码**:
```python
def _worker_day_logic(...):
    # 在现有逻辑后增加
    
    # 如果背包满了石头且围墙已建完
    if worker.backpack_full and not walls_missing:
        stones = worker.backpack.count(WALL_MATERIAL)
        if stones >= 10:  # 一次卖10个石头
            # 需要判断是否在商店附近
            vendor_pos = _find_vendor(turn)
            if vendor_pos and distance(worker.pos, vendor_pos) <= 1:
                commands[worker.unit_id] = sell_command(WALL_MATERIAL, num=10)
                return
```

---

### 8. 实现LLM决策辅助

**用途**: 在复杂决策时调用LLM获取建议

**修改**: `server.py`和`brain.py`

**示例**:
```python
def decide(payload: dict[str, Any]) -> tuple[dict[str, dict[str, Any]], str]:
    """
    主决策函数
    返回: (指令字典, LLM prompt)
    """
    turn = Turn.load(payload)
    commands: dict[int, dict[str, Any]] = {}
    llm_prompt = ""
    
    # 在特定情况下生成LLM prompt
    if turn.round_no % 130 == 1:  # 每天开始
        llm_prompt = _generate_strategy_prompt(turn)
    
    if turn.is_day:
        _decide_day(turn, commands)
    else:
        _decide_night(turn, commands)
    
    return commands, llm_prompt
```

**server.py修改**:
```python
# 第57行
response, llm_prompt = decide(payload)

# 第60-64行
full_response = {
    "roleCommandMap": response,
    "prompt": llm_prompt,  # 使用实际的prompt
    "executeCmd": "",
}
```

---

### 9. 性能监控与统计

**功能**: 记录每回合的决策耗时和资源使用情况

**实现**:
```python
# server.py中增加性能统计
import time

def do_POST(self) -> None:
    start_time = time.perf_counter()
    
    try:
        # ... 原有逻辑 ...
        
        response = decide(payload)
        
        elapsed_ms = (time.perf_counter() - start_time) * 1000
        LOGGER.info("performance id=%d round=%d elapsed=%.2fms commands=%d",
                    req_id, round_no, elapsed_ms, len(response))
    
    except Exception:
        # ...
```

---

## 📋 实施计划

### 第一周（必做）

- [ ] Day 1: 创建run.sh脚本并测试
- [ ] Day 2: 修复日志体积问题
- [ ] Day 3: 增加边界检查
- [ ] Day 4-5: 进行集成测试，使用docs/request.txt验证
- [ ] Day 6-7: 编写单元测试

### 第二周（重要）

- [ ] Day 1-3: 完善开拓者任务系统
- [ ] Day 4-5: 补充函数文档注释
- [ ] Day 6-7: 代码review和重构

### 第三周（可选）

- [ ] Day 1-2: 实现资源交易策略
- [ ] Day 3-4: 实现LLM决策辅助
- [ ] Day 5-7: 性能优化和压力测试

---

## ✅ 验收标准

### 基础功能
- [ ] 能够正确启动（bash run.sh 8000）
- [ ] 能够接收并解析判题系统的请求
- [ ] 能够生成合法的响应指令
- [ ] 不会因为异常而崩溃
- [ ] 日志文件大小可控（<100MB/1000回合）

### 策略表现
- [ ] 白天能够建造3座武器塔
- [ ] 白天能够建造围墙防御体系
- [ ] 夜晚能够操控武器攻击机器人
- [ ] 单位不会因为碰撞而停止移动

### 代码质量
- [ ] 所有单元测试通过
- [ ] 代码符合PEP 8规范
- [ ] 关键函数都有文档注释
- [ ] 没有明显的性能瓶颈

---

## 🔧 工具脚本

### 快速测试脚本 (test_client.sh)
```bash
#!/bin/bash
# test_client.sh - 快速测试客户端

PORT=8000

# 1. 启动客户端（后台）
cd CoreGeek
./run.sh $PORT > test.log 2>&1 &
PID=$!

# 2. 等待启动
sleep 2

# 3. 发送测试请求
curl -X POST http://localhost:$PORT \
  -H "Content-Type: application/json" \
  -d @../docs/request.txt \
  -o response.json

# 4. 检查响应
if [ -f response.json ]; then
    echo "✅ Response received"
    cat response.json | python -m json.tool
else
    echo "❌ No response"
fi

# 5. 停止客户端
kill $PID

echo "Log:"
tail -20 test.log
```

### 日志分析脚本 (analyze_log.py)
```python
#!/usr/bin/env python3
"""分析debug.log，统计决策耗时"""

import json
import re
from pathlib import Path

def analyze_log(log_file: Path):
    rounds = []
    current_round = None
    
    with log_file.open('r', encoding='utf-8') as f:
        for line in f:
            if 'request_decoded' in line:
                match = re.search(r'round=(\d+)', line)
                if match:
                    current_round = int(match.group(1))
            
            if 'response_sent' in line and current_round:
                rounds.append(current_round)
                current_round = None
    
    print(f"Total rounds processed: {len(rounds)}")
    print(f"Rounds range: {min(rounds)} - {max(rounds)}")

if __name__ == '__main__':
    analyze_log(Path('CoreGeek/debug.log'))
```

---

## 📞 联系与支持

如果在实施过程中遇到问题：

1. 查看代码审查报告.md的详细说明
2. 参考设计文档.md的架构设计
3. 查阅docs/接口文档.md的API规范

---

**清单创建日期**: 2026-09-12  
**预计完成时间**: 2-3周  
**优先级排序**: 🔴 > 🟡 > 🟢
