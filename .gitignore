# 日志与运行时产物
debug.log
*.log
**/.processed_issues.json

# 自动化系统回收站（失败任务的未跟踪文件存放处，见 git_pusher._quarantine）
# 忽略整个 .claude/：git 对未跟踪目录只报目录名不递归，
# 写成 .claude/discarded/ 匹配不到
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# 打包产物
CoreGeek_*.tar.gz

# 密钥（对应设计文档11.7节：禁止提交token）
github_token.txt
githubtoken.txt
automation/github_token.txt
.env
