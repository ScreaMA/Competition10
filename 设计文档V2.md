# 《未来战争》编程大赛客户端设计文档 V2

> 版本：V2.0
> 日期：2026-09-15
> 状态：**推翻重来**（V1 的 `CoreGeek/src/agent/brain.py` 7114 行单文件实现全部废弃）
> 依据：`docs/任务书.md`、`docs/接口文档.md`、`docs/request.txt`、`docs/response.txt`、
> GitHub Issues #1–#199（110 份对战复盘报告 + 182 条 claude-prompt 修复建议）、
> `战术参考/`（王相鉴聊天记录与布局图）

---

## 目录

1. [V2 要解决什么：V1 的失败清单](#1-v2-要解决什么v1-的失败清单)
2. [设计原则](#2-设计原则)
3. [系统架构](#3-系统架构)
4. [协议层](#4-协议层)
5. [世界模型](#5-世界模型)
6. [自进化任务子系统（重点）](#6-自进化任务子系统重点)
7. [白天策略](#7-白天策略)
8. [夜晚策略](#8-夜晚策略)
9. [日志与可观测性](#9-日志与可观测性)
10. [测试策略](#10-测试策略)
11. [兼容性与约束](#11-兼容性与约束)
12. [实施路线](#12-实施路线)

---

## 1. V2 要解决什么：V1 的失败清单

V1 的实现方式是"每轮对战复盘 → 让 Claude 根据复盘改 `brain.py`"的循环。182 条修复建议
全部落在同一个文件上，导致 `brain.py` 膨胀到 7114 行、包含 300+ 个函数和大量只服务
于某一场对局的特殊分支（`PK590252`、`PK591684`、`PK591772` 等对局编号直接写在注释和
判据里）。这些补丁互相打架，形成了下面这些**至今仍在复现**的问题。

### 1.1 P0：自进化任务全线崩溃（直接决定胜负）

对战数据里，任务分是唯一能稳定拿分的手段（单任务 80 分 + 80 金币，15 回合超时），
而我们几乎每场都是 **0 任务分**。逐条列出复盘证据：

| # | 现象 | 复现对局 | 根因 |
|---|------|---------|------|
| T1 | 沙盒读题成功（`key=yes`、`docs=2`、`urls=2`、`exitCode:0`），但 `api=0` —— **从未真正调用过任务 API** | PK591684 R11–R13、PK591772 R12–R13、PK592172 R12–R16、PK592173 R11–R16 | 状态机把"读题"当成了终点。读题分支没有出口条件，`no_api_data` 状态下每回合重新发一遍读文件命令 |
| T2 | 接取任务后 **7–9 个回合从未 `submitAnswer`** | PK592172 / PK592173 | 没有"必须交出点什么"的兜底。答案提取失败时静默不发指令，任务超时归零 |
| T3 | 调 API 时**缺鉴权头**，R14 吃 401 `Missing 'Authorization' header` | PK592173 | 任务文档里写了鉴权方式（`API_DOCS.md` 里是 `Authorization: Bearer …`），但没有从文档里提取并复用 |
| T4 | 沙盒反复重读同一份文档，输出逐字节相同，`watch r1/t2→t6`、`fail=8` | 几乎每场 | 没有任何"这一步没产生新信息"的判据。看门狗只数回合，不看内容 |
| T5 | 任务 2 全程可接**从未接取** | PK592172 / PK592173 | 任务调度写成了"接一个做完再说"，第一个卡死后整场不再看第二个任务点 |
| T6 | 接任务后 `commands 3→2`，`idle_man` 峰值 3 | PK592173 | 开拓者被任务独占，另外两个工人没有兜底工作 |
| T7 | 提交的"答案"是任务文档原文 / 401 错误 JSON | PK590557 R14/R16/R18 | 答案提取没有校验闸门，任何非空字符串都能被交上去 |

**最关键的一条**：任务书 5.3 节明确写着自进化类任务的**设计意图**是

> 玩家需要根据任务1探索的内容，形成固定 SOP 或者 SKILL，实现 Agent 自进化，
> 进而快速做出后续任务。

也就是说"查询北京天气 → 查询上海天气"这类同族任务，**第二次必须比第一次快**。
V1 里没有任何"跨任务复用"的结构——`_TASK_ANSWER_CACHE` 只按任务文件名缓存最终答案，
换个城市（换任务文件）就完全失效。V2 必须把这条做成架构级的核心能力。

### 1.2 P1：经济冻结与防线缺失

| # | 现象 | 证据 | 根因 |
|---|------|------|------|
| E1 | 金币连续 7–10 回合恒为 0 | PK592172 R8–R16、PK592173 R9–R16 | 开局 75 金三个回合全部砸进 3 座塔，之后无任何收入手段；背包里的 stone/copper 攒着不卖 |
| E2 | 采集 R3 一次后断档 9–10 回合 | PK592172（R9 采 1 石，R10–R13 中断） | 采集没有专职化，工人被建造/任务的临时目标反复打断 |
| E3 | 围墙全程 0–1 段，唯一一段还是孤立错位 | PK592172 R15 @(33,12)、PK592173 R15 @(33,12) | 围墙是"顺路捡起来建"，不是有计划的成线封堵 |
| E4 | 三塔贴基地 1–2 格同侧，零升级 | 几乎每场：`(30,8)/(29,9)/(30,11)` | 塔位常量写死在基地旁；没有可达性校验，没有前出覆盖来路 |
| E5 | 首塔 R4 才落地，敌方 R1 就连建 2 座 | PK592173 | 开局 R1–R3 纯 `move` 探路 |

### 1.3 P2：工程性问题

- `brain.py` 单文件 7114 行，300+ 函数，无法审查、无法定位问题。
- 判据散落在注释里，新补丁与旧补丁冲突（例如"读文件死循环"和"没找到任务文件"
  被混成同一条止损判据，导致真正该重试的回合被提前熔断——见 `_task_fetch_failed`
  函数里长达 60 行的自我辩解注释）。
- 大量"反推常量名"式的修复建议（复盘报告只能猜 `TOWER_LOADOUT`、`STONE_BATCH`
  这些名字），说明代码结构与策略意图已经无法从外部读懂。

### 1.4 V2 的取舍

**保留**：V1 积累下来的、经过对战验证的**领域知识**（任务书规则表、战术参考结论、
对战复盘里查证过的道具名/价格/伤害公式/task 家族结构）。

**废弃**：V1 的**全部代码结构与全部特殊分支**。

**新增**：自进化任务的 SOP/SKILL 复用机制、沙盒输出指纹与进度判据、答案校验闸门、
可建造区在线学习。

---

## 2. 设计原则

### P1 — 无状态决策，有状态学习

战场决策**每回合从请求报文重新推导**，不依赖跨回合的战场缓存（V1 的好设计，保留）。
唯一允许跨回合累积的是**学习结果**：技能库（SOP/SKILL）、可建造区模式、任务族的
已知 API 档案。学习结果只增不减、只在得到沙盒确证后才写入。

### P2 — 确定性优先，LLM 兜底

任务书的接口里 `prompt`/`llmResp` 是每日限额 3 次的资源，但**自进化任务执行期间不计入
限额、不受限制**（`docs/接口文档.md` §1.7 errorCode=5 的注）。因此：

- **白天/夜晚的战术决策完全不用 LLM**。V1 的 `_llm_plan`（让 LLM 回 `PLAN: tower=3
  wall=2 …`）产生的是"LLM 建议与指令脱节"的经典故障，且每个游戏日只够问 3 次，
  问出来的还是猜。删掉。
- **LLM 只用在自进化任务里**，且只作为"确定性阶梯全部失败后"的兜底顾问。任务期间
  免费，所以可以每回合发一次 prompt 换取下一回合的 `llmResp`，与确定性 `executeCmd`
  并行不冲突（两个字段互不占用）。

### P3 — 每一条指令都必须能回答"为什么"

任何下发的 `executeCmd` 都必须来自一个**具名的策略步骤**（`Step`），且该步骤必须声明
它期望看到的证据（`expect`）。拿不到证据的步骤不能重复执行第二次。

### P4 — 有界探索，必有终态

任务链路的每一个循环都必须有硬上限，且必须能到达终态：`SUBMIT` 或 `ABANDON`。
"没做出答案就不发指令"是不允许的——`submitAnswer` 有部分分（`通过率 = 正确字段数 /
全量字段数`），**交一份部分正确的答案永远优于不交**。

### P5 — 学习要能被证伪

技能库里的一条 SKILL 带着它被验证成功的次数。若某条 SKILL 连续失败 2 次，
自动降级出阶梯；若某条经验与沙盒实测冲突（比如记着 `X-API-Key` 却吃到 401），
以沙盒实测为准并覆盖。

### P6 — 模块边界即测试边界

每个模块都必须能脱离 HTTP 服务器单独构造输入测试。`brain.decide(payload) -> response`
是唯一的集成面。

---

## 3. 系统架构

### 3.1 模块图

```
CoreGeek/
├── main3.py                       # 入口：解析端口、切工作目录、配置日志、启动服务
├── run.sh                         # 判题系统调用方式：bash run.sh <port>
├── pyproject.toml
├── src/agent/
│   ├── __init__.py
│   ├── protocol.py                # 规则常量表 + 报文解析 + 指令构造（无策略）
│   ├── grid.py                    # 几何与寻路：切比雪夫距离、A*、邻居、连通性
│   ├── world.py                   # 世界模型：由 Turn 派生的决策视图（含可建造区学习）
│   ├── brain.py                   # 编排器：decide(payload) -> response
│   ├── server.py                  # HTTP 服务 + 结构化日志
│   └── strategy/
│       ├── __init__.py
│       ├── economy.py             # 采集 / 贩卖 / 金币分配
│       ├── defense.py             # 塔位与墙线规划、夜晚火力控制
│       └── task/
│           ├── __init__.py
│           ├── memory.py          # 跨回合持久层：FACTS + SKILLS + TaskRun
│           ├── scripts.py         # 注入沙盒的 shell/python 片段模板
│           ├── sandbox.py         # executeCmd 传输、标记解析、输出指纹
│           ├── answer.py          # 答案提取与校验闸门
│           ├── skills.py          # 任务族识别 + SOP 阶梯
│           └── solver.py          # 任务状态机（观测→计划→执行→反馈）
├── tests/
└── tools/
```

### 3.2 数据流

```
判题系统 POST / ──► server.Handler
                     │
                     ├─► protocol.Turn.load(payload)      报文 → 强类型快照
                     │
                     ├─► world.World(turn)                快照 → 决策视图
                     │      ├─ buildable.weapon_at(off)  可建造区（学习出来的）
                     │      ├─ threat.sides()            敌方来路
                     │      └─ routes.step_to(...)       A* 寻路
                     │
                     ├─► strategy.task.solver.plan(world, memory)
                     │      └─► TaskPlan{ accept | execute | submit | abandon | idle }
                     │
                     ├─► strategy.defense.plan_day/night(world)   战斗指令
                     ├─► strategy.economy.plan_day(world)         经济指令
                     │
                     └─► brain.assemble(...) ──► Response{roleCommandMap, prompt, executeCmd}
```

`brain.assemble` 负责冲突消解：**一个角色一回合只能有一条指令**（接口文档 §2.3 注）。
分配顺序为 任务 > 建造 > 采集/贩卖 > 移动兜底。

### 3.3 与 V1 的结构对比

| 维度 | V1 | V2 |
|------|----|----|
| 决策代码 | `brain.py` 7114 行单文件 | 8 个模块，单模块 ≤ 700 行 |
| 任务链路 | 与战斗决策混在同一函数族，靠 `if TASK_*` 分支互相插入 | 独立包 `strategy/task/`，对外只暴露 `plan()` 一个函数 |
| 跨回合状态 | `_TASK_ANSWER_CACHE` / `_TASK_WATCH` 两个全局变量，语义写在注释里 | `memory.Memory` 显式对象，分 `FACTS` / `SKILLS` / `RUN` 三区，全部可序列化 |
| 止损判据 | 5 种失败形态各有一套 `repeats/fails/probes/submits` 计数 | 统一为 **输出指纹 + 步骤预算**，一个判据 |
| 沙盒命令 | 一个巨型 `TASK_EXECUTOR` 字符串（700 行），靠 30 个占位符替换 | 每个 `Step` 一个独立脚本模板，互不干扰 |
| 答案提交 | 先缓存、再校验、失败重试，逻辑分散在 6 个函数 | `answer.gate()` 单点闸门，`SUBMIT` 是状态机的显式终态 |

---

## 4. 协议层

### 4.1 常量表（`protocol.py`）

严格照抄任务书，**不接受推测值**：

```
DAY_ROUNDS = 70      NIGHT_ROUNDS = 60      ROUNDS_PER_DAY = 130
MAX_ROUNDS = 1300

WEAPON_COST = 25     WALL_COST_STONE = 1
MAX_WEAPONS = 3      背包：开拓者 40 / 工人 100

# 单位类型
STATION / GATLING / RAILGUN / ROCKET / WALL / PIONEER / WORKER
# 机器人类型
SMALL / MIDDLE / LARGE / BOSS  → 威胁（攻击力, 射程, HP, 积分）
   small  (5, 3, 40, 1)   middle (10, 3, 60, 2)
   large  (20, 3, 500, 4) boss   (40, 3, 800, 10)

# 满血表（任务书 4.5.1）
STATION_HP = {1: 1500, 2: 3000, 3: 4500}
WALL_HP    = {1: 1000, 2: 1500, 3: 2000}
GATLING    = 攻击力 10×等级, 射程 (3, 5, 7)
RAILGUN    = 攻击力 10/20/30, 射程 (6, 8, 10)   # 穿透：能量按伤害扣减
ROCKET     = 攻击力 20×等级, 射程 (10, 15, ∞), 冷却 3 回合，落点 3×3 溅射（中心 20，外圈 10）

# 中立元素
stone / iron / copper / vendor / weaponShop
{challenger,defender}TaskPoint{1,2}

# 商店道具（`docs/request.txt` 实证在位）
WeaponUpgradeVoucher1/2  WallUpgradeVoucher1/2  StationUpgradeVoucher1/2
WallFixer  Medicine  DizzyWeapon  Bomb
Small/Middle/Large/BossRobotSummonOrder
AcientTablet StarSand FlameBreath FrostPotion ThornAmulet IronWhistle
```

`RAILGUN`/`ROCKET` 的多目标规则（任务书 §4.5.4）：**加特林/火箭的 `targetPos` 个数必须
等于当前等级**（level 1 → 恰好 1 个，level 3 → 恰好 3 个），加特林的多目标必须落在
同一 90° 锥内。指令构造器 `attack_command(controller_id, weapon, targets)` 在
`protocol.py` 里强制这个约束（不足时用合法占位点补齐，超出时截断），**避免因为目标
个数不对导致整次攻击非法**。

### 4.2 报文解析

`Turn.load(payload)` 解析接口文档 §1.1 的**全部**字段。V1 漏解析了 `errors`、`llmResp`、
`lastSummonTreasureResult`，V2 全部补上：

```python
@dataclass(frozen=True, slots=True)
class Turn:
    round_no: int
    is_day: bool
    team_type: str                 # challenger / defender
    gold: int
    total_score: int
    width: int
    height: int
    zones: dict[Pos, str]
    ours: tuple[Unit, ...]
    enemies: tuple[Unit, ...]
    robots: tuple[Robot, ...]
    player_tasks: tuple[PlayerTask, ...]
    phase_task: str                # 当前已领取任务的原文描述（任务活性判据）
    last_cmd_result: str           # 上回合 executeCmd 结果
    last_action_results: dict[int, bool]
    last_summon_result: int
    llm_resp: str
    errors: tuple[Error, ...]
    vendor_shop: tuple[ShopItem, ...]
    weapon_shop: tuple[ShopItem, ...]
```

`last_cmd_result` 的格式约定（接口文档 §1.1）必须严格解析：

| 前缀 | 含义 | 处理 |
|------|------|------|
| `[exitCode:N]` | 正常结束，N 为退出码 | 正常解析正文 |
| `[TIMEOUT]` | 超过 15 秒被掐断 | **不计异常**（接口文档 §2.1），但下半条命令要降预算重发 |
| `[JUDGER_ERROR]` | 判题器侧异常 | 不计异常，原样重试 |
| 正文末尾 `[TRUNCATED]` | 输出超 64KB 被截断 | 解析器标记 `truncated`，答案提取要容忍尾部缺失 |

### 4.3 指令构造

每种动作一个构造函数，参数名与接口文档 §2.2 完全一致：

```python
move_command(pos)                     -> {"action":"move","targetPos":[pos]}
attack_command(ctrl_id, weapon, pts)  -> {"action":"attack","controllerId":str(ctrl_id),
                                          "targetPos":[...]}   # 个数由 weapon.level 决定
build_command(pos, name)              -> {"action":"build","targetPos":[pos],"name":name}
remove_command(pos) / collect_command(pos) / use_command(name, pos=None, num=None)
sell_command(name, num) / buy_command(name, num)
accept_task_command() / submit_answer_command(text) / summon_treasure_command(pos, items)
```

**统一约束**：`roleCommandMap` 的 key 必须是**字符串**（接口文档 §2.1 是 `Map<int,…>`，
JSON 序列化后是字符串键，V1 的 Demo 用 `str(key)`，V2 沿用）。

---

## 5. 世界模型

`world.World(turn)` 是每回合构造一次的只读视图，承载所有派生计算。

### 5.1 可建造区：在线学习（V2 新增）

**问题**：接口报文里**没有**任何字段说明哪些格子可以建造。任务书只给了规则
（蓝区只能建武器、黄区只能建围墙），`战术参考/基地6x6可建造区.png` 给出的是
"基地周边 6×6"这个量级，但**蓝黄两区的具体划分在图上无法可靠反推**
（实测中 `(29,9)` 能建武器，`(32,12)` 能建围墙，两者都在基地 6×6 范围内且不构成
同心环——说明分区不是简单的"外环黄、内区蓝"）。

**方案**：不猜。把可建造区当成**相对基地的固定模式**在线学习：

```python
# 以基地 2×2 footprint 的 (xmin, ymin) 为原点，记录每个相对偏移的可建性
WEAPON_ZONE: dict[Pos, bool]   # 相对偏移 -> 能否建武器（True/False 都记）
WALL_ZONE:   dict[Pos, bool]   # 相对偏移 -> 能否建围墙
```

- **写入时机**：`lastRoundRoleActionResults[unit_id] == True` 的 `build` 指令 ⇒ 该偏移
  可建；`== False` ⇒ 该偏移不可建（或该格被占，见下）。
- **失败消歧**：`build` 失败可能是"格子非法"也可能是"目标格被占/距离不对"。
  只有**目标格确认空闲、角色确认相邻、金币/材料充足**时的失败才写入 `False`，
  否则丢弃这次观测。
- **使用**：塔位/墙位候选先从"已学到的 `True`"里取；候选不足时，按距离基地的
  切比雪夫半径由近到远探测半径 ≤ 3 的格（**每个回合最多探测 1 个未知格**，
  探测本身也是一次正常的建造尝试，失败不浪费——那个工人本来也没有更好的事做）。
- **收益**：V1 里塔位是写死的常量，换边（上下半场互换位置）后完全失效；
  V2 的塔位随基地位置自动平移，且会避开"建成后堵死通道"的格子（见 5.4）。

### 5.2 威胁面

```python
class Threat:
    enemy_sides: frozenset[str]   # up/down/left/right，敌方主力来向
    nearest_robot: Pos | None
    robot_clusters: list[Cluster] # 用于火箭溅射与炸弹落点
```

敌方来向优先用**可见的敌方基地**（全图可见）相对我方基地的方位推算；不可见时退回
最近的敌方单位；都没有时退回"地图中心方向"。V1 的 `_enemy_sides` 逻辑正确，V2 保留
并简化。

### 5.3 路径与连通性（`grid.py`）

- `distance(a, b)`：切比雪夫距离。
- `step_toward(world, unit, goal, reserved)`：A*（八方向，切比雪夫代价），返回第一步；
  `reserved` 是本回合已被其他角色认领的格子，避免自家人抢同一格。
- `reachable(world, unit, goal, extra_blocked)`：只做可达性判定（BFS），
  用于 5.4 的连通性预演。
- **移动碰撞规则**（任务书 §4.5.4）在 `step_toward` 里体现：目标格若被
  "本回合不移动的己方角色/机器人占据"则不可入；对角线穿越两个障碍之间是允许的
  （八方向移动不受"拐角"限制）。

### 5.4 布局可达性（战术参考 T2，V1 已实现但耦合在巨型函数里）

选定塔位组合后做一次**建成预演**：

```python
def layout_ok(world, sites) -> bool:
    blocked = set(sites)                      # 假设塔全部建成
    for site in sites:
        stands = [n for n in neighbors(site) if world.land(n) and n not in blocked]
        if not stands:                        # 这座塔被自己的邻居堵死
            return False
        if not any(world.reachable(origin, s, blocked) for s in stands):
            return False                      # 有人能到，但从基地出发走不过去
    return True
```

塔位候选按"朝敌方来向 > 地图内侧 > 其他"排序，取前 3 个，若 `layout_ok` 为假则
换下一个组合（候选 ≤ 6 个，穷举 20 种组合的成本可忽略）。

**这就是聊天记录里"很容易出现炮塔把路堵住然后有一个炮塔碰不到"的直接对策。**

---

## 6. 自进化任务子系统（重点）

这是 V2 投入最多设计的部分，也是**唯一能稳定拉开分差**的地方。

### 6.1 任务书对自进化的定义

> 开拓者前往 2 个任务点接取自进化类任务，根据任务描述需要，与沙盒环境进行交互，
> 最终提交答案信息。
> 玩家需要根据任务 1 探索的内容，形成固定 **SOP 或者 SKILL**，实现 **Agent 自进化**，
> 进而快速做出后续任务。

三条硬约束：

1. **沙盒一回合只能下发一条 `executeCmd`**，执行 ≤ 15 秒，输出 > 64KB 截断。
2. **沙盒无外网**，只有本地服务（实测 `http://localhost:8899`）与基础 shell/python。
3. **任务有时限**（实测 `timeoutRounds = 15`），且开拓者**离开任务点一格内即结束任务**。

第 3 条决定了任务链路是一个**短回合预算内的有限步规划问题**：15 个回合，每回合一步，
必须在前 4–6 步内出结果并提交，剩下的余量用来做部分分补救。

### 6.2 实测到的任务形态（来自对战日志）

#### 家族 A：`1-fixed-step/1-unknown-api`（API 查询族）

任务文件：`/tmp/selfEvolutionTask/1-fixed-step/1-unknown-api/task_1_<城市>.md`

实测任务原文（PK592172 报文里的 `lastCmdResult` 原文摘录）：

```
# 自进化任务 A-1：查询北京文化遗产
系统提供了一个 API 服务（运行在 http://localhost:8899），API 文档在 API_DOCS.md 中。
注意：由于该系统经过了长期迭代，文档中的部分字段内容已经发生变化，描述不再准确
（其它内容可以认为是准确的）。

## 任务要求
从 API 查询北京市的全部文化遗产记录，然后通过 submitAnswer 提交：
{"city":"北京","total_count":<总记录条数>,
 "world_heritage_count":<保护级别为"世界遗产"的数量>,
 "types":["<所有不重复的遗产类型，顺序不限>"],
 "oldest_era":"<年代最早的遗产名称>"}
- 提交答案为数字/字符串敏感型，不能将数字 0 写成 "0"，否则算错
```

从对战日志中已经**确证**的接口事实（V2 直接内置为种子知识，但仍以实测为准）：

| 项 | 值 | 来源 |
|----|----|------|
| 端点 | `http://localhost:8899/api/v1/heritage/search` | 对战日志 |
| 鉴权 | `Authorization: Bearer heritage-api-key-2024` | 对战日志（`X-API-Key` 会 401） |
| 查询参数 | `location=<城市名>`（**不是 `city`**，用 `city` 会 400）、`limit`、`page` | 对战日志 |
| 响应 | `{"code":200,"data":{"records":[…],"pagination":{"total_count":N,…}}}` | 对战日志 |
| 记录字段 | `id, name, type, era, protected_level`（**不是 `protection_level`**） | 对战日志 |
| 保护级别取值 | `"世界遗产"` / `"国家级"` / `"省级"` | 对战日志 |
| 陷阱 | 文档是**过时的**：字段名/参数名/鉴权方式至少有一处是错的，必须以实测为准 | 任务原文明确写出 |

任务书的示例（"查询北京天气 / 上海天气 / 广州天气"）说明这一族的核心形态是
**同一套接口 + 不同参数**——这正是 SKILL 复用的主战场。

#### 家族 B：`1-fixed-step/2-engineering-fix`（工程修复族）

任务文件：`/tmp/selfEvolutionTask/1-fixed-step/2-engineering-fix/task_1_<代号>.md`，
旁边有工作区 `ws_1/`。实测形态（PK 日志与复盘交叉验证）：

- 工作区里有配置（如 `config/alpha.conf`）、日志目录（`logs/alpha/`）、检查脚本 `./check`。
- 需要**先修 CRLF**：`sed -i 's/\r$//' check && chmod +x check`，否则
  `bad interpreter: /bin/sh^M`。
- 按规范改配置、改权限，跑 `./check`，输出出现 `[ OK ]` 后从中提取 `TOKEN:` 值，
  用 `submitAnswer` 提交该 token。

#### 家族 C：未知族

任务书只给了两个例子，沙盒路径里的 `1-fixed-step` 前缀暗示还有 `2-…`、`3-…` 等更高
阶的家族。**V2 不能假设只有 A 和 B**，必须有一条对未知族也成立的通用发现流程。

### 6.3 核心机制：观测 → 技能 → 计划 → 执行 → 反馈

```
        ┌──────────────────────────────────────────────────────────┐
        │                     Memory（跨回合持久）                  │
        │  FACTS  : 键值事实（API 地址/鉴权头/字段名/era 序 …）      │
        │  SKILLS : 任务族 → 已验证的 SOP（步骤序列 + 参数 + 战绩）  │
        │  RUN    : 当前任务的活动状态（含输出指纹历史）             │
        └──────────────────────────────────────────────────────────┘
                 ▲ 写                        │ 读
                 │                           ▼
   ┌─────────┐  │  ┌──────────┐  ┌──────────────┐  ┌──────────┐  ┌────────┐
   │ 观测     │──┴─►│ 族识别    │─►│ 计划（阶梯） │─►│ 执行      │─►│ 反馈    │
   │ observe │     │ classify │  │ ladder       │  │ execute  │  │ judge  │
   └─────────┘     └──────────┘  └──────────────┘  └──────────┘  └────────┘
        ▲                                                              │
        └────────────────────── 下一回合 lastCmdResult ────────────────┘
```

#### 6.3.1 观测（Observe）

每回合从报文取三样东西：

```python
@dataclass(frozen=True)
class Observation:
    in_task: bool          # phase_task 非空 ⇒ 任务进行中
    task_text: str         # phase_task 原文
    task_key: str          # 由 task_text 归一化出的任务标识（用于换任务检测）
    sandbox: SandboxOutput # lastCmdResult 解析结果
```

`SandboxOutput` 解析 `last_cmd_result`，抽出所有**结构化标记**（见 6.6）：

```python
@dataclass(frozen=True)
class SandboxOutput:
    status: str            # ok / timeout / judge_error / empty
    truncated: bool
    raw: str
    markers: dict[str, str]      # 单值标记：RECON.root / API.status …
    records: list[dict]          # [DATA] 行里的记录（可选）
    answer: str | None           # [ANSWER] 行里的最终答案
    fingerprint: str             # raw 的归一化哈希（见 6.4）
```

#### 6.3.2 族识别（Classify）

**不依赖沙盒内容也能分类**——先用任务文本，再用沙盒输出修正：

```python
def classify(task_text: str, recon: ReconFacts | None) -> str:
    text = task_text.lower()
    if "http" in text or "api" in text:            return "api-query"
    if "spec" in text or "修复" in text or "./check" in text:  return "engineering-fix"
    if recon and recon.has("check_script"):        return "engineering-fix"
    if recon and recon.has("http_base"):           return "api-query"
    return "unknown"
```

任务文本里出现 `task_1_alpha.md`、`规格`、`修复` 等词 → 工程修复；出现
`查询`、`API`、`接口` → API 查询。识别结果连同**证据**一起写进 `Memory.FACTS`，
下一个同族任务可以直接跳过识别阶段。

#### 6.3.3 技能库（SKILLS）

```python
@dataclass
class Skill:
    family: str                    # api-query / engineering-fix / …
    signature: str                 # 归一化特征串（用于匹配同族任务）
    steps: tuple[StepSpec, ...]    # 已验证的步骤序列
    facts: dict[str, str]          # 该族已验证的事实（可覆盖默认值）
    wins: int = 0                  # 成功次数
    losses: int = 0                # 连续失败次数
```

**SKILL 的写法**：不是自然语言 SOP，而是**可执行的步骤序列**——每一步都是一个
参数化的沙盒命令模板 + 期望证据 + 后继选择器。这样"形成固定 SOP"就不是文档里
的一句话，而是下一场对局里**真的能跑的命令**。

**SKILL 的生成**：任务成功提交后，把这次实际走通的步骤序列（含每一步的实际参数）
提升为 `Skill`。若已有同族 SKILL，则做**合并**：把新参数并入 `facts`、
把更短的路径替换进 `steps`、`wins += 1`。

**SKILL 的复用**：新任务开始时先按 `signature` 查 SKILL。命中时**从第 0 步开始按
缓存的步骤执行**，期望 2–3 回合内闭环（而不是重新探索）。这就是任务书要求的
"快速做出后续任务"。

#### 6.3.4 计划：阶梯（Ladder）

每个族对应一条**有序的步骤阶梯**。计划器每回合挑"当前应该执行的那一步"：

```python
def next_step(run: Run, skill: Skill | None, ladder: Ladder) -> Step | None:
    # 1) 有 SKILL 且未被证伪 → 按 SKILL 的步骤走
    # 2) 否则按族阶梯走，跳过已经拿到证据的步骤
    # 3) SKILL 连续失败 2 次 → 降级回阶梯
```

**家族 A 的阶梯**（`api-query`）：

| 步 | 名称 | 沙盒命令做什么 | 期望证据 |
|----|------|---------------|---------|
| A0 | `recon` | 定位任务目录、读任务文件、列出候选文档 | `[RECON] task=… docs=…` |
| A1 | `discover` | 读 `API_DOCS*`/`*.md`，抽出 base_url / endpoint 候选 / 鉴权候选 / 参数名候选 | `[API] base=… auth=… param=…` |
| A2 | `query` | **直接调接口**：对候选矩阵（base×auth×param 各 2–3 个）逐个请求，命中即分页取全量 | `[DATA] n=…` |
| A3 | `answer` | 由全量记录聚合出答案字段，打印 `[ANSWER]{json}` | `[ANSWER] …` |
| **A4** | `submit` | **无需沙盒**：把 `[ANSWER]` 交给 `submitAnswer` 下发 | 判题器接受 |

**关键设计**：`A2` 与 `A3` **合并进同一条沙盒命令**。V1 的死循环正是"读题成功但
从不调 API"——把"调 API"做成 `A1` 之后**必然执行的下一步**，物理上就不存在
"停在读题阶段"的状态。

**家族 B 的阶梯**（`engineering-fix`）：

| 步 | 名称 | 做什么 | 期望证据 |
|----|------|-------|---------|
| B0 | `recon` | 定位任务文件 + 工作区 `ws_*/`，读 spec/README | `[RECON] ws=… spec=…` |
| B1 | `normalize` | `sed -i 's/\r$//'` 修复工作区内脚本换行符 + `chmod +x` | `[FIX] crlf=n chmod=m` |
| B2 | `check` | 跑 `./check`（或 spec 里点名的入口），收集失败输出 | `[CHECK] ok=no err=…` |
| B3 | `repair` | 按 spec 与 check 输出的差异，应用**目标修复**（改配置项/改权限/改路径） | `[FIX] file=… key=…` |
| B4 | `verify` | 重跑 check，抓 `[ OK ]` 与 `TOKEN:` | `[TOKEN] …` |
| **B5** | `submit` | 提交 token | 判题器接受 |

`B1→B4` 最多循环 `ENGINEERING_MAX_ROUNDS` 次（默认 4）；每次循环内**允许把
"归一化+跑 check"打包成一条命令**（15 秒内足够），所以实测 3–4 个回合可以完成。

**家族 unknown 的阶梯**：`recon` → （可选）`llm_advice` → `generic_probe`
（读所有文档、跑所有可执行文件、抽取任何形如 token/答案的值）→ `submit`。

#### 6.3.5 执行（Execute）

每回合最多下发**一条** `executeCmd`。命令由 `scripts.py` 的模板填充参数生成，
统一以 `PYEOF` heredoc 或者 `sh -c` 包裹，并遵守：

- **时间预算**：脚本内部有软预算（`TIME_BUDGET = 9s`），HTTP 超时 2s × 候选数；
  宁可少试几个候选，也不能让整条命令被 15 秒掐掉（被掐掉 = 这一回合零信息）。
- **输出纪律**：所有诊断信息走**结构化单行标记**（6.6），正文超过
  `BODY_LIMIT=4000` 字符即截断并打 `[CLIP]`，保证不撞 64KB 上限。
- **解释器探测**：`for P in python3 python; do command -v "$P" …done`，两个名字都试。
- **无状态**：脚本本身不留跨回合状态；跨回合记忆由**客户端**持有（`Memory`），
  需要时通过参数注入脚本。这样沙盒被重置也不会丢学习成果。

#### 6.3.6 反馈与进度判据（**V2 的核心修复点**）

这是 V1 最大的漏洞。V2 用**一个**统一判据替代 V1 的五套计数：

```python
def progress(run: Run, out: SandboxOutput) -> Progress:
    if out.answer:                    return Progress.ANSWER_READY
    if out.fingerprint == run.last_fingerprint:   return Progress.REPEATED
    if out.status == "timeout":       return Progress.SLOW
    if out.status == "judge_error":   return Progress.RETRYABLE
    if out.markers_advanced(run):     return Progress.ADVANCED
    return Progress.NO_INFO
```

处理规则：

| 判据 | 动作 |
|------|------|
| `ANSWER_READY` | **立即转 `submit` 状态**，本回合就发 `submitAnswer` |
| `ADVANCED` | 记指纹，前进到阶梯的下一步 |
| `REPEATED` | **同一份输出连续出现 2 次即判定该步失败**，跳过它前进到下一步；这一步从 SKILL 里划掉 |
| `SLOW` | 把脚本时间预算减半重发（最多 1 次），仍超时则跳步 |
| `NO_INFO` | 记指纹，允许**同一回合内**加宽一次探测矩阵（换候选），再不行就跳步 |
| `RETRYABLE` | 原样重发，不计消耗 |

`fingerprint` 的定义：对 `raw` 做归一化（去掉时间戳、行序无关化、压缩空白）后取
sha1 前 12 位。**只对"应当产生新信息"的步骤计算指纹**（`recon`/`discover`/`query`/
`check`），`normalize`/`repair` 这类幂等步骤不参与。

#### 6.3.7 终态与止损

任务链路必须收敛到其中之一：

| 终态 | 触发条件 |
|------|---------|
| `DONE` | 判题器接受了答案（`phaseTask` 变为空 / 分数上涨 / `errors` 无 errorCode=2） |
| `ABANDON` | 见下 |

**放弃条件**（满足任一即放弃，并**本回合就发 `submitAnswer` 交最后一次拿到的答案**，
然后让开拓者离开任务点）：

1. 已用回合数 ≥ `timeoutRounds - SAFETY_MARGIN`（安全余量 2 回合）。
2. 阶梯全部走完且没有任何 `[ANSWER]`。
3. 连续 3 个回合 `REPEATED` 且当前阶梯已无下一步。
4. SKILL 连续失败 2 次 → 先降级回阶梯；阶梯也失败 → 放弃。
5. 基地血量低于 40% 且开拓者已提交过至少一次答案 → 放弃并回防。

**"放弃前必提交"是硬规则**：只要手上有过任何 `[ANSWER]`（哪怕不完整），
放弃前一定发一次 `submitAnswer`。部分分 = `任务积分奖励 × 通过率`，交比不交严格更优。

### 6.4 输出指纹与去重

```python
_NORM = re.compile(r"\d{4}-\d{2}-\d{2}[ T][\d:.,]+|\b\d{6,}\b|\s+")

def fingerprint(raw: str) -> str:
    norm = _NORM.sub(" ", raw).strip()
    return hashlib.sha1(norm.encode("utf-8", "replace")).hexdigest()[:12]
```

去掉时间戳与长数字（任务 ID、耗时），压缩空白，再哈希。目的只有一个：
**"这两回合沙盒说的是一模一样的话" ⇒ 这一步卡死了**。

### 6.5 任务调度（谁去做、什么时候做）

```python
def schedule(world, mem) -> TaskPlan:
    """返回 accept / advance / wait / idle"""
```

规则：

1. **只要存在 `isValid=True` 的任务点，就要有人在做任务**——这是首要原则
   （V1 因为"做完一个再说"导致任务 2 整场未接）。
2. 当前无任务且开拓者存活 → 选**最近且 `coldDownRounds == 0`** 的任务点，
   派开拓者前往；相邻则 `acceptTask`。
3. 有任务在身 → 每回合推进一级阶梯（`executeCmd`），开拓者**原地不动**
   （离开即结束任务）。**执行 `executeCmd` 不需要角色动作**，所以开拓者的
   `roleCommandMap` 条目可以空着，也可以顺手做点别的——但**不能离开任务点一格内**。
4. 夜间：开拓者默认**继续驻守任务点**（任务期间沙盒与 LLM 都免费，任务是收益
   最高的动作）。仅当"基地受威胁 + 已有提交"时放弃任务回防。
5. 任务结束后进入 30 回合冷却；冷却期间开拓者回防（白天帮建/采，夜晚操控武器）。
6. **开拓者的指令不挤占其他角色**：V1 的 `commands 3→2` 是因为把开拓者算成了
   战斗单位；V2 里两个工人始终按经济/防御逻辑出满指令，`idle_man` 不应上升。

### 6.6 沙盒结构化标记（协议）

沙盒脚本与客户端之间的**唯一接口**。所有标记形如 `[TAG] key=value …` 或
`[TAG] payload`，独占一行，便于正则解析与指纹计算。

| 标记 | 含义 | 示例 |
|------|------|------|
| `[RECON]` | 侦察结果 | `[RECON] root=/tmp/selfEvolutionTask/1-fixed-step/1-unknown-api task=/…/task_1_beijing.md docs=2 ws=0` |
| `[DOC]` | 文档内容片段 | `[DOC] path=/…/API_DOCS.md\n<正文>` |
| `[SCAN]` | 目录扫描摘要 | `[SCAN] files=7 dirs=3 py=yes sh=yes` |
| `[API]` | 一次接口调用 | `[API] url=http://localhost:8899/api/v1/heritage/search status=200 auth=Bearer n=42` |
| `[APIFAIL]` | 一次失败的接口调用 | `[APIFAIL] url=… status=401 reason=missing_header` |
| `[DATA]` | 取到的记录集摘要 | `[DATA] n=137 total=137` |
| `[SOLUTION]` | 候选答案（可能不完整） | `[SOLUTION] {"city":"北京", …}` |
| `[ANSWER]` | **最终答案**（校验过的） | `[ANSWER] {"city":"北京","total_count":137,…}` |
| `[FIX]` | 工程修复动作 | `[FIX] crlf=3 chmod=2 file=config/alpha.conf key=port` |
| `[CHECK]` | 检查脚本结果 | `[CHECK] ok=no code=1 err=bad interpreter` |
| `[TOKEN]` | 工程修复族要提交的值 | `[TOKEN] a3f1c9…` |
| `[CLIP]` | 输出被主动截断 | `[CLIP] dropped=8123` |
| `[DONE]` | 脚本正常收尾（可用于判断"跑到头了"） | `[DONE] steps=5 elapsed=3.2s` |

**约定**：脚本**必须以 `[DONE]` 收尾**。没有 `[DONE]` 的输出 = 脚本被中途掐断，
这一回合的证据不完整，反馈层按 `SLOW`/`NO_INFO` 处理而不是 `REPEATED`
（避免把"被掐断"误判成"死循环"——这正是 V1 `_task_fetch_failed` 里
五种形态纠缠不清的根源）。

### 6.7 答案提取与校验闸门

`answer.py` 是**唯一**允许产出 `submitAnswer` 载荷的地方。

```python
def gate(candidate: str | None, task_text: str, out: SandboxOutput) -> str | None:
    """返回可提交的答案；None 表示不许提交"""
```

闸门依次检查：

1. **非空**、长度 ≥ 8。
2. **不是文档回声**：与任务原文/文档正文的相似度（去空白后的最长公共子串占比）
   > 0.6 直接拒绝——这正是 V1 把任务书原文交上去的原因。
3. **不是错误体**：命中 `401/403/404/500`、`{"error": …}`（值为非空）、
    `Traceback`、`No such file`、`Permission denied` 一律拒绝。
    *（V1 在这里踩过一次假阳性：`{"error": null}` 被判成错误体。V2 的判据明确
    要求"值非空"才对上，且用 `json.loads` 结构化判定，不做裸词匹配。）*
4. **字段名匹配**：从任务原文里提取 JSON 模板的键集合（正则抓 `{…}` 块并
   `json.loads` 试解析），要求候选答案的键集合 ⊇ 模板键集合中**已知语义**的部分。
   缺字段不算失败（部分分），但**一个字段都没对上的**要拒绝。
5. **类型保真**：任务原文若写了"不能将数字 0 写成 \"0\""，则检查数值字段确实
   是 JSON number 而非 string。判断方式：模板里该键的 placeholder 是 `<…>` 形式
   或示例值类型。
6. **去重**：与 `run.submitted` 里已交过的答案比较，完全相同则不重复提交
   （浪费回合），除非上一次被 `errorCode=2` 打回。

**提交失败的闭环**：若 `submitAnswer` 之后 `phaseTask` 仍在且 `errors` 出现
`errorCode=2`，说明答案被判错。此时：
- 把该答案写进 `run.rejected`，**禁止再次提交**；
- 反馈层强制跳到阶梯的 `query` 步重取数据（而不是重交同一个答案）——
  V1 的 "复读同一答案直到额度烧完"（PK592108 R15–R17）由此消除。

### 6.8 LLM 顾问（仅任务期间）

任务期间 LLM 免费且不限次，所以每回合都发一条 prompt，拿到的是**上一回合**的
`llmResp`。使用规则：

- prompt 内容 = 任务原文 + 最近 2 回合的沙盒标记（**只发标记，不发全文**，
  避免 64KB 级 prompt）+ 当前阶梯位置 + 明确的输出要求
  （`直接给出一条可在 sh 中执行的命令`或`直接给出 submitAnswer 的 JSON`）。
- `llmResp` 到达后**不直接执行**，而是交给 `scripts.sanitize_llm_command()`：
  只接受单条命令、长度 ≤ 800、不含 `rm -rf`/`mkfs`/`dd`/`shutdown` 等高危模式、
  不含网络外联。**校验后的命令以最高的阶梯优先级执行一次**，并把结果并入证据。
- 这条路径只作为阶梯全失败后的兜底，**不计入 SKILL**（命令来源不可控，
  不满足 P5"学习要能被证伪"——它无法解释为什么成立）。

### 6.9 与 V1 的逐条对照（自进化部分）

| V1 故障 | V2 对策 | 代码位置 |
|---------|---------|---------|
| T1 `api=0` 死循环 | 阶梯里 `discover → query` 是**必然的下一步**；`query` 内含候选矩阵，不依赖 discover 成功 | `skills.py: API_LADDER` / `scripts.py: api_query_script` |
| T2 从未提交 | 终态强制 `SUBMIT`；放弃前必提交；`gate` 允许部分分 | `solver.py: _finish` |
| T3 缺鉴权 401 | `discover` 抽 key；`query` 的候选矩阵**同时**试 `Authorization: Bearer` / `X-API-Key` / 无头三种；401 自动换 | `scripts.py: _AUTH_VARIANTS` |
| T4 反复重读 | 输出指纹 + `REPEATED` 判据，2 次即跳步 | `solver.py: progress` |
| T5 任务 2 未接 | 调度原则"只要 isValid 就要有人在做" | `solver.py: schedule` |
| T6 指令数骤降 | 任务只占用开拓者，工人指令独立出满 | `brain.py: assemble` |
| T7 交文档原文 | `answer.gate` 的"文档回声"与"错误体"检查 | `answer.py` |
| — | **新增**：SKILL 复用，同族第二个任务 2–3 回合闭环 | `memory.py` / `skills.py` |

---

## 7. 白天策略

### 7.1 开局（R1–R5）：这一条直接来自复盘

V1 的 R1–R3 纯 `move` 探路、R4 才落第一座塔，而敌方 R1 就连建 2 座。
V2 的开局指令顺序：

```
R1: 金币 75 ≥ 25 ⇒ 立即建第一座塔（命名为 rocket，射程 10 优先级最高）
    worker2 去最近的石矿；worker2 到位后每回合 collect 直到背包 ≥ 2 石
    pioneer 直奔最近的有效任务点
R2-R5: 继续建塔（金币 75 够 3 座）；塔位优先朝敌方来向的前出点
```

**塔型顺序 `(ROCKET, RAILGUN, GATLING)`** 保留 V1 结论（射程 10 → 6 → 3，
导弹优先，见 `战术参考/修改建议.md`）。同型号是否可以重复建造未确认，V2 保持
三种各一座，并把"试探第二座 rocket"作为可开关的常量
（`TOWER_LOADOUT_ALLOW_DUP = False`）。

### 7.2 经济：专职分工 + 卖矿保活

复盘里最刺眼的是"金币冻结 7–10 回合"。V2 的经济规则：

| 角色 | 固定职责 | 说明 |
|------|---------|------|
| worker1 | **建造工** | 按建造队列走：塔 > 围墙 > 升级 |
| worker2 | **采集工** | 常驻矿区采集，**不在建造/采集之间反复切换** |
| pioneer | **任务手** | 任务优先；无任务时回防 |

卖出规则（`SELL_RULES`）：

```python
def sell_plan(world, worker) -> list[str]:
    """返回该工人本回合建议卖出的矿石清单"""
    need_stone = 未完成的围墙段数          # 每段 1 石
    gold_short = world.gold < 25          # 连一座塔都买不起
    plan = []
    if gold_short:
        plan += ["copper", "iron"]        # 卖价 5 / 3，优先卖贵的
    if worker.stone_count > need_stone + SELL_STONE_RESERVE:
        plan.append("stone")              # 囤超过需求的部分才卖
    return plan
```

**兜底**：`gold == 0` 且背包有矿石 ⇒ 本回合**无条件**卖一次（哪怕只有 1 块石头）。
这条规则直接消灭"金币恒 0"。

### 7.3 建造队列

```
优先级 0：紧急——基地血量 < 40% 且金币 ≥ 100 ⇒ 买 StationUpgradeVoucher（回满血 + 升级）
优先级 1：武器塔未满 3 座 且 金币 ≥ 25 ⇒ 建塔
优先级 2：围墙未达 WALL_TARGET_SEGMENTS（默认 8）段 且 有石材 ⇒ 建墙（先封敌向来路）
优先级 3：金币 ≥ 100 且已有 2 座塔 ⇒ 买 WeaponUpgradeVoucher1 升级塔
优先级 4：金币 ≥ 20 且检测到残血墙 ≥ 3 段 ⇒ 买 WallFixer 修墙
优先级 5：金币 ≥ 10 且角色残血 ⇒ 买 Medicine
```

金币**预留**规则：建塔前保证卖出后余额 ≥ 下一优先级的最低花费，避免"花光最后一金"
（V1 的 R9 一次花光 25 金导致之后 7 回合冻结）。

### 7.4 围墙规划

- 目标段数 `WALL_TARGET_SEGMENTS = 8`（覆盖来向两侧 + 正面）。
- 位置：以基地为中心的第二圈，**按敌方来向排序**，来向侧优先、入口开在最后一位
  （V1 `_wall_ring` / `_wall_entrance` 的思路正确，V2 保留并简化）。
- 每段都需要 1 块石头在**建造者的背包**里，所以"围墙"和"采集"必须联动：
  建造工发现背包没石头就去最近石矿（或找采集工要——报文里查不到别人的背包，
  所以直接自己采）。
- 布局**必须**通过 §5.4 的可达性预演。

---

## 8. 夜晚策略

### 8.1 配位

3 个可操控角色（开拓者 + 2 工人）× 最多 3 座武器。分配算法：

```python
def pair(world) -> list[tuple[Unit, Unit]]:
    """把可操控角色与武器配对，最大化"已就位"的数量"""
```

- 已经在武器相邻格的配对优先保留（避免每晚重新跑位）。
- 剩余角色按"到武器的最短距离"做贪心匹配（≤ 3 座武器，直接枚举全排列即可）。
- 任务中的开拓者**不参与**配对（它要留在任务点），由 `TASK_NIGHT_RELEASE`
  决定是否在特定条件下释放。

### 8.2 目标选择

按武器类型分别计算最优 `targetPos`：

- **加特林**：射程内机器人按距离排序，取前 `level` 个（level 1 → 1 个）。
  多个目标必须落在同一 90° 锥内 ⇒ 先选"威胁最高的一个"作为锚点，
  再在锚点 ±90° 的半平面内挑其余目标；挑不够就用锚点重复填充（合法但无效，
  不会触发指令错误）。
- **电磁狙击炮**：只传 1 个目标。选**弹道上机器人最多**的落点（穿透机制下，
  一条线上排队的机器人吃满伤害）。枚举射程内所有机器人位置作为落点，
  打分 = 线上机器人在剩余能量内的伤害总和。
- **火箭发射台**：`cooldown > 0` 时该武器不攻击（换角色去堵墙/待命）。
  否则选 **3×3 溅射覆盖威胁值最高**的落点（威胁值 = Σ 机器人 `attackPower ×
  剩余血量系数`），多枚导弹（高等级）各选一个互不重叠的最优落点。

### 8.3 空转兜底

- 射程内无目标 ⇒ 该角色去**修墙/堵缺口**（`plug_wall_gap`：站在围墙缺口上，
  用人肉城墙挡一回合，见 `战术参考/人肉城墙堵缺口.png`）。
- 无缺口可堵 ⇒ 靠近受损最重的围墙或基地待命。

---

## 9. 日志与可观测性

日志是复盘流水线（`automation/`）的唯一输入，V2 必须保持并增强它。

### 9.1 每回合一行（`request_decoded`）

```
id=17 round=17 team=4334 team_type=defender roles=8 gold=0 score=0
base=(30,10) hp=1500/1500
towers=rocket1@(29,9),railgun1@(30,11),gatling1@(30,8)
walls=1[l1:1] robots=0(s0 m0 l0 b0) robot_pos=[] mines=14
tasks=[自进化类1@(23,14)可接/15回/80分 自进化类2@(26,17)冷却12]
phase="请阅读task_1_beijing.md" task_state=query step=A2 r=4/15
bag=10010{stone:2} 10011{} 10012{copper:1}
```

### 9.2 决策后一行（`strategy_done`）

```
id=17 round=17 commands=4 elapsed=3.86ms
actions=10010:build→(32,12) wall  10011:submitAnswer "<答案全文>"  10012:collect→(34,15)
sandbox=下发 step=A2 cmd="python3 - <<'PYEOF'…" result=(待下回合)
llm=ask/-  内存: skills=1 facts=6
```

### 9.3 沙盒结果一行（`sandbox_result`）

```
id=18 round=18 text=[exitCode:0]
markers={RECON.root=…, API.status=200, DATA.n=137}
answer=yes fp=9a3c1f2e0d44 progress=ANSWER_READY
```

### 9.4 每日汇总（`day_summary`）

```
day=1 rounds=1-130 gold_in=180 gold_out=150 kills=42 deaths=0
tasks=2/2 完成=1 提交=3 通过率=0.8 击杀分=96 任务分=80
windows: 塔=3 墙=8 升级=2 空转回合=6
```

**要求**：`tools/analyze_log.py --template` 能直接从这些行渲染出
`战术参考/对战分析模板.md` 的空格，不需要人工填表。

### 9.5 隐私/体积

- `debug.log` 记 DEBUG 全量，`stdout` 只记 INFO。
- 沙盒正文在 stdout 里最多打 500 字符 + 标记摘要，全文进 `debug.log`。

---

## 10. 测试策略

### 10.1 分层

| 层 | 文件 | 覆盖内容 |
|----|------|---------|
| 协议 | `tests/test_protocol.py` | 报文解析（含 `errors`/`llmResp`/`[TIMEOUT]`/`[TRUNCATED]`）、指令构造（尤其 `attack` 的 `targetPos` 个数 = 等级） |
| 几何 | `tests/test_grid.py` | 切比雪夫距离、A* 绕障、对角穿越、可达性预演 |
| 世界 | `tests/test_world.py` | 可建造区学习（写入/消歧/兜底探测）、威胁面推导 |
| 经济 | `tests/test_economy.py` | 卖矿规则、金币预留、建造优先级队列 |
| 防御 | `tests/test_defense.py` | 塔位可达性、墙线顺序、三种武器的目标选择、冷却处理 |
| 任务-沙盒 | `tests/test_sandbox.py` | 标记解析、指纹归一化、`[DONE]` 缺失处理 |
| 任务-答案 | `tests/test_answer.py` | 闸门 6 条规则，重点覆盖"文档回声"与 `{"error": null}` 假阳性 |
| 任务-技能 | `tests/test_skills.py` | 族识别、阶梯推进、SKILL 生成/合并/证伪 |
| 任务-状态机 | `tests/test_solver.py` | 端到端回放：用真实对战日志片段构造 `Turn` 序列，断言"3 回合内出 `submitAnswer`"、"指纹重复 2 次即跳步"、"放弃前必提交" |
| 集成 | `tests/test_brain.py` | `decide(payload)` 的黄金用例 + 不变量（每角色 ≤ 1 指令、key 为字符串） |

### 10.2 回归用例（来自真实故障）

`tests/test_solver.py` 里用**真实对战日志**构造的用例，每个对应一个 P0 故障：

| 用例 | 断言 |
|------|------|
| `test_api_task_submits_within_3_rounds` | 给定 PK592172 的 R10–R12 报文序列（含 `key=yes` 但 `api=0` 的沙盒输出），第 3 回合的响应必须含 `submitAnswer` |
| `test_repeated_output_advances_step` | 同一份沙盒输出连续 2 回合 ⇒ 第 2 回合下发的命令与第 1 回合**不同** |
| `test_auth_retry_on_401` | 沙盒回 `401 Missing Authorization` ⇒ 下一回合的命令里含 `Authorization` 或 `X-API-Key` |
| `test_abandon_still_submits` | 任务进入放弃态时，本回合响应必须含 `submitAnswer`（若有候选答案） |
| `test_doc_echo_rejected` | 把任务原文当答案 → `gate` 返回 `None` |
| `test_error_null_not_rejected` | `{"error": null, …}` 是合法答案（V1 的假阳性回归） |
| `test_second_task_accepted` | 任务 1 结束后，任务 2 `isValid` ⇒ 开拓者前往任务点 |
| `test_skill_reuse_second_task` | 先跑一遍家族 A 成功 ⇒ `Memory` 里生成 SKILL ⇒ 第二个同族任务从缓存的 `query` 步开始，且**首回合就下发 query 命令**（不再 recon） |
| `test_tower_reachability` | 三塔堵死唯一通道的地图 ⇒ 换位后 `layout_ok` 为真 |
| `test_no_gold_freeze` | `gold=0` 且背包有矿石 ⇒ 本回合指令含 `sell` |
| `test_first_round_builds_tower` | `roundNo=1`、`gold=75` ⇒ 指令含 `build` |

### 10.3 运行

```bash
cd CoreGeek && python -m pytest tests/ -q
```

> 自动化流水线（`automation/`）的 `claude-prompt` 约束里要求"修改后必须通过
> `cd CoreGeek && python -m pytest tests/ -q`"，V2 保持该命令与路径不变。

---

## 11. 兼容性与约束

### 11.1 不动的东西（硬约束）

| 项 | 约束 |
|----|------|
| `automation/` | **完全不动**。自动化流水线是独立系统，与客户端无代码耦合（已核实 `automation/*.py` 不引用 `CoreGeek/`）。 |
| `docs/` | 只读，作为规格来源。 |
| 入口 | `bash run.sh <port>` → `python main3.py <port>` → 监听 `0.0.0.0:<port>` → `POST /`（任意路径）→ JSON 响应。**签名与行为完全不变。** |
| 依赖 | **零第三方依赖**（`dependencies = []`）。沙盒与判题环境不可假设有 pip。只用标准库。 |
| Python | ≥ 3.11（`run.sh` 里的版本校验不变）。 |
| 响应字段名 | `roleCommandMap` / `prompt` / `executeCmd`，与接口文档 §2.1 一致。 |
| 报文字段名 | 严格按接口文档，**不做任何拼写兼容性猜测**（V1 里 `protected_level` /
  `protection_level` 这类猜测是任务侧的事，不是协议侧的事）。 |

### 11.2 异常预算（任务书 §8）

单队异常响应累计 5 次即被停调度，这是**最致命的失败模式**。V2 的防护：

- **指令字段完整性**：`attack` 必须有 `controllerId` 与 `targetPos`；`use` 眩晕法宝/
  范围炸弹必须有 `targetPos`；`move`/`build`/`collect`/`remove` 必须有 `targetPos`。
  这些在 `protocol.py` 的构造函数里**强制**，构造不出合法指令时返回 `None`
  而不是发一条半成品（构造函数返回 `None` ⇒ 该角色本回合不出指令）。
- **值域校验**：`targetPos` 必须在 `[0,width) × [0,height)` 内；
  `attack` 的落点数必须等于武器等级；`num` 必须是正整数。
- **动作合法性预检**：白天不发 `attack`；非工人不发 `build`/`collect`/`remove`；
  非开拓者不发 `acceptTask`/`submitAnswer`/`summonTreasure`；
  不在任务中不发 `executeCmd`。
- **兜底**：`brain.decide` 整体包在 `try/except` 里，异常时返回
  `{"roleCommandMap": {}, "prompt": "", "executeCmd": ""}`（空指令不是异常，
  只是这一回合不做动作）。

> **注意**：`executeCmd` 超时与沙盒故障**不计入异常次数**（接口文档 §2.1），
> 所以任务探索在异常预算上是安全的；真正要防的是**指令构造错误**。

### 11.3 视野的诚实处理

报文只给可见的敌方单位（敌方基地与围墙全局可见）。**V2 不对不可见信息做任何假设**：
推断来敌方向只用"可见的敌方基地"或"最近的可见敌方单位"，两者都没有时，
按"地图中心方向"降级——这是唯一合理的先验。

---

## 12. 实施路线

| 阶段 | 内容 | 交付 |
|------|------|------|
| 1 | `protocol.py` / `grid.py` / `world.py` | 报文 → 强类型世界视图，含可建造区学习 |
| 2 | `strategy/economy.py` / `strategy/defense.py` | 白天/夜晚的确定性策略 |
| 3 | `strategy/task/*` | 自进化任务子系统（本文档第 6 章） |
| 4 | `brain.py` / `server.py` / `telemetry.py` | 编排、冲突消解、结构化日志 |
| 5 | `tests/` | 10 个测试文件，含 11 个真实故障回归用例 |
| 6 | `tools/` | `local_check.py`（真实报文自检）、`simulate.py`（本地对局）、`analyze_log.py`（复盘渲染） |

### 12.1 验收标准

1. `cd CoreGeek && python -m pytest tests/ -q` 全绿。
2. `bash CoreGeek/tools/test_client.sh` 能对 `docs/request.txt` 返回合法响应。
3. `tests/test_solver.py` 的 11 个回归用例全部通过——这 11 个用例对应的正是
   Issues #1–#199 里反复出现的 11 类故障。
4. 无第三方依赖；`automation/` 与 `docs/` 零改动。

### 12.2 交付清单（V2.0，2026-09-15）

| 文件 | 行数 | 职责 |
|------|------|------|
| `CoreGeek/main3.py` | 64 | 入口（判题系统契约不变） |
| `CoreGeek/run.sh` | 43 | 启动脚本（判题系统契约不变） |
| `src/agent/protocol.py` | 816 | 规则常量表 + 报文解析 + 指令构造 |
| `src/agent/grid.py` | 270 | 几何、A*、可达性、锥形判定 |
| `src/agent/world.py` | 334 | 世界模型 + 可建造区在线学习 |
| `src/agent/telemetry.py` | 182 | 逐回合增量（只服务日志） |
| `src/agent/brain.py` | 285 | 编排器 |
| `src/agent/server.py` | 161 | HTTP + 决策超时保护 |
| `src/agent/strategy/economy.py` | 561 | 经济与建造执行 |
| `src/agent/strategy/defense.py` | 558 | 塔位/墙线/夜战 |
| `src/agent/strategy/task/*` | 3025 | 自进化任务子系统（其中 `scripts.py` 1130 行，绝大部分是注入沙盒的脚本模板） |
| `tests/*.py` | 3257 | 236 个用例（11 个真实故障回归） |
| `tools/*.py` | 1009 | 自检 / 模拟 / 复盘 |

客户端源码合计 **6216 行**（V1：`brain.py` 7114 行 + `protocol.py` 512 +
`grid.py` 104 + `server.py` 540 ≈ 8270 行）。

对照 V1：`brain.py` 从 **7114 行的单文件**拆成 10 个模块，**单模块 ≤ 816 行**
（`scripts.py` 是例外：1130 行里绝大部分是给沙盒用的独立脚本，不参与客户端逻辑）。

**实测数据**（`tools/local_check.py` + `tools/simulate.py`）：

| 指标 | 结果 |
|------|------|
| 单回合决策耗时 | 平均 0.61ms，最慢 1.1ms（判题器给的窗口是 5 秒） |
| 1300 回合累计 | 0.79s |
| 本地模拟 400 回合 | 4.1ms/回合，任务链路 recon→query→submit 闭环 12 次 |
| 异常预算 | 全部指令由构造器生成；值域与角色-动作匹配在 `test_brain.py` 里逐条断言 |

### 12.3 演进方向（V2 之后）

- **技能库落盘**：当前 `Memory` 存活于客户端进程内（覆盖一场比赛的两个半场）。
  若要跨比赛复用，需要写入 `CoreGeek/.skills.json`。风险是判题环境的文件系统
  可能每次重置——先用"进程内 + 可选落盘"的方式，落盘失败静默降级。
  `Memory` 已经预留了 `to_json` / `from_json`。
- **同型号多塔**：确认规则后把 `TOWER_LOADOUT` 改成 `(ROCKET, ROCKET, RAILGUN)`。
- **家族 C 及以上**：沙盒路径里的 `1-fixed-step` 前缀提示存在更高阶家族。
  V2 的通用发现流程（`recon → generic → LLM 兜底`）是它们的兜底，
  但一旦实测到具体形态，应按家族 A/B 的方式固化成专属阶梯。
- **`timeoutRounds` 的自适应**：当前截止回合按任务点声明的超时值推算。
  如果实测发现某些族天然需要更多回合，可以按族配置不同的安全余量。
- **半场之间的技能复用**：判题器是否在同一进程里跑完两个半场尚未确认。
  如果是，`Memory` 天然跨半场；如果不是，需要落盘。
